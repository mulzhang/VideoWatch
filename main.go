package main

func main() {
	//student := stu.NewStudent()
	//sendHttp := student.SendHttp(`userid=`+student.Userid, "POST", "http://wljy.whut.edu.cn/edu/eduStuCourse/findEduStuCourse.ajax")
	//kpo := course.GetKpo(sendHttp, student)
	//m := course.GetGroup(kpo, student)
	//mysql := datasource.ConnectMysql()
	//if len(m) != 0 {
	//	var wg sync.WaitGroup
	//	wg.Add(len(m))
	//	for k, v := range m {
	//		go func(key string, value []*course.RequestStudy) {
	//			defer wg.Done()
	//			if len(value) > 0  {
	//				for _,v :=range value {
	//					byUrl := video.GetMediaDurationByUrl(v.VideoUrl)
	//					v.VideoTime = byUrl
	//
	//				}
	//				datasource.AddRecord(mysql,value)
	//			}
	//		}(k, v)
	//	}
	//	wg.Wait()
	//}
	//i := 1
	//ticker := time.NewTicker(2 * time.Second)
	//for _ = range ticker.C {
	//	i++
	//	fmt.Println(i)
	//}
}
